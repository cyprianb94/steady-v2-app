export const STEADY_AI_FREEZE_ACTIVE = true;

export const STEADY_AI_FREEZE_TITLE = 'Steady AI is paused';

// Product guardrail: do not surface proactive AI notes, nudges,
// conversations, or LLM calls until this freeze is deliberately lifted.
export const STEADY_AI_FREEZE_MESSAGE =
  'Steady AI is paused while the feature is rebuilt. Plan, sync, run review, and recovery tools still work normally.';
