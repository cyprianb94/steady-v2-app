/**
 * Re-export from shared types package.
 * This file exists so existing server imports don't break.
 */
export { propagateChange } from '@steady/types';
export type { PropagateScope } from '@steady/types';
